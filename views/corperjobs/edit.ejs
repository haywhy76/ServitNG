<% include ../partials/header %>


<div class="ui main text container ">
        <div class="editjobheading">Edit Listing</div>
        <form class="ui form" action="/corperjobs/<%=corperjob._id%>?_method=PUT" method="POST"> 

                <div class="field">
                        <input type="text" name="corperjob[title]" value="<%= corperjob.title %>">
                </div>
                <div class="field">
                        <input type="text" name="corperjob[location]" value="<%= corperjob.location %>">
                 </div>
                 <div class="field">
                        <input type="text" name="corperjob[company]" value="<%= corperjob.company %>">
                 </div>
                 <div class="field">
                        <input type="text" name="corperjob[salary]" value="<%= corperjob.salary %>">
                 </div>
               
                <div class="field">
                        <textarea name="corperjob[description]"> <%= corperjob.description %> </textarea>
                </div>

                
                <p class="editbutton"><button type="submit" class="btn btn-default btn-lg loginsubmitbutton">UPDATE </button></p>
                
        </form>
</div>

        
